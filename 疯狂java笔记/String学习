	1.String类没有提供 修改  字符串的方法，所以，String类对象成为不可变字符串。
	  String get = "Hello";---->只能这么修改 get = get.subString(0,2)+"fu";--->Hefu
	2.不可变字符串的好处:编译器可以让字符串共享（修改一个字符串 和  重新创建一个字符串 的效率问题）
	
	3.检测字符串是否相等：s.equals(t); 不能使用==来判断两个字符串是否相等。==只判断两个引用是否相同